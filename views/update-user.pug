extends layout 
block content 
  body.background-chicks
    .container.mt-2
      .signup-container
        .row.justify-content-center
          .col-lg-12.col-md-10
            .card
              .card-header
                .container
                  .row
                    .col-2
                      img.chicken-pic.mt-1(src='/img/chicken.jpg' alt='Logo')
                    .col-10
                      h2.mt-2 Join Young4ChickS
                      p Update user details:
              .card-body
                // Tab Navigation
                ul#formTabs.nav.nav-tabs.mb-4(role='tablist')
                  li.nav-item(role='presentation')
                    button#personal-tab.nav-link.active.signup-nav-tabs(data-bs-toggle='tab' data-bs-target='#personal' type='button' role='tab') Personal Info
                  li.nav-item(role='presentation')
                    button#contact-tab.nav-link.signup-nav-tabs(data-bs-toggle='tab' data-bs-target='#contact' type='button' role='tab') Contact Details
                  li.nav-item(role='presentation')
                    button#recommender-tab.nav-link.signup-nav-tabs(data-bs-toggle='tab' data-bs-target='#recommender' type='button' role='tab')
                      | Recommender
                      | Details
                  li.nav-item(role='presentation')
                    button#account-tab.nav-link.signup-nav-tabs(data-bs-toggle='tab' data-bs-target='#account' type='button' role='tab') Account Settings
                // Form with Tab Content
                form(ation='/updateUser?id='+ user._id method='POST')
                  #formTabsContent.tab-content
                    // Personal Info Tab
                    #personal.tab-pane.fade.show.active(role='tabpanel' aria-labelledby='personal-tab')
                      .mb-3
                        label.form-label(for='firstName') First Name
                        input#firstName.form-control(name='firstName' placeholder='--Enter your first name--' type='text' value=user.firstName)
                      .mb-3
                        label.form-label(for='lastName') Last Name
                        input#lastName.form-control(name='lastName' placeholder='--Enter your last name--' type='text' value=user.lastName)
                      .mb-3
                        label.form-label(for='age') Age (18-30)
                        input#age.form-control(name='age' placeholder='--Enter your age--' type='number' value=user.age)
                      .mb-3
                        label.form-label(for='gender') Gender
                        select#gender.form-control(name='gender')
                          option(value='male' selected = user.gender==='male') Male
                          option(value='female' selected = user.gender==='female') Female
                          option(value='notStated' selected = user.gender==='notStated') Prefer not to say
                          option(value='Other' selected = user.gender==='Other') Other
                      .mb-3
                        label.form-label(for='nin') National ID Number
                        input#nin.form-control(name='nin' placeholder='--Enter your national ID number--' type='text' value=user.nin)
                      .d-flex.justify-content-between
                        a(href='/login')
                          button.btn.btn-secondary.prev-tab(type='button' data-prev-tab='personal-tab')
                            i.fa-solid.fa-arrow-left
                            |  Back to
                            |                                                     login
                        button.btn.btn-primary.next-tab(type='button' data-next-tab='contact-tab')
                          | Next 
                          i.fa-solid.fa-arrow-right
                    // Contact Details Tab
                    #contact.tab-pane.fade(role='tabpanel' aria-labelledby='contact-tab')
                      .mb-3
                        label.form-label(for='email') Email Address
                        input#email.form-control(name='email' placeholder='--Enter your email address--' type='email' value=user.email)
                      .mb-3
                        label.form-label(for='phone') Phone Number
                        input#phone.form-control(name='phone' placeholder='--Enter your phone number--' type='tel' value=user.phone)
                      .mb-3
                        label.form-label(for='address') Address
                        textarea#address.form-control(name='address' placeholder='--Enter your address--' rows='3' type='text' value=user.address)
                      .d-flex.justify-content-between
                        button.btn.btn-secondary.prev-tab(type='button' data-prev-tab='personal-tab')
                          i.fa-solid.fa-arrow-left
                          |                                                 Previous
                        button.btn.btn-primary.next-tab(type='button' data-next-tab='recommender-tab')
                          | Next 
                          i.fa-solid.fa-arrow-right
                    // Recommender Details Tab
                    #recommender.tab-pane.fade(role='tabpanel' aria-labelledby='recommender-tab')
                      .mb-3
                        label.form-label(for='recommenderName') Recommender Name
                        input.form-control(name='recommenderName' type='text' placeholder="--Enter recommender's name--" value=user.recommenderName)
                      .mb-3
                        label.form-label(for='recommenderEmail') Email Address
                        input.form-control(name='recommenderEmail' type='email' placeholder="--Enter recommender's email address--" value=user.recommenderEmail)
                      .mb-3
                        label.form-label(for='recommenderPhone') Phone Number
                        input#phone.form-control(type='tel' name='recommenderPhone' placeholder="--Enter recommender's phone number--" value=user.recommenderPhone)
                      .mb-3
                        label.form-label(for='recommenderAddress') Address
                        textarea#address.form-control(rows='3' placeholder="--Enter recommender's Address--" name='recommenderAddress' type='text' value=user.recommenderAddress)
                      .d-flex.justify-content-between
                        button.btn.btn-secondary.prev-tab(type='button' data-prev-tab='contact-tab')
                          i.fa-solid.fa-arrow-left
                          |                                                 Previous
                        button.btn.btn-primary.next-tab(type='button' data-next-tab='account-tab')
                          | Next 
                          i.fa-solid.fa-arrow-right
                    // Account Settings Tab
                    #account.tab-pane.fade(role='tabpanel' aria-labelledby='account-tab')
                      .mb-3
                        label.form-label(for='role') Role
                        select#role.form-control(name='role')
                          option(value='farmer' selected = user.role==='farmer') Farmer
                          option(value='salesRep' selected = user.role==='salesRep') Sales Representative
                          option(value='brooderManager' selected = user.role==='brooderManager') Brooder Manager
                      .d-flex.justify-content-between
                        button.btn.btn-secondary.prev-tab(type='button' data-prev-tab='recommender-tab')
                          i.fa-solid.fa-arrow-left
                          |                                                 Previous
                        button.btn.btn-success(type='submit')
                          i.fa-solid.fa-user
                          |                                                 Update
    // Bootstrap JS Bundle with Popper
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')
    script(src='/scripts/signupScript.js')
